<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Show</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
  <div class="main w-full min-h-screen bg-zinc-600 p-10 text-white">
    
    <a href="/" class="text-blue-500 inline-block mb-10">Go Back To Home</a>
    
    <h1 class="text-4xl tracking-tighter"><%= filename %></h1>
    
    <p class="text-xl mt-2 font-light whitespace-pre-wrap">
      <%= filedata %>
    </p>

    <!-- Delete Button -->
    <form action="/delete/<%= filename %>" method="POST" class="mt-10">
      <button
        type="submit"
        onclick="return confirm('Are you sure you want to delete this file?')"
        class="px-5 py-2 bg-red-600 hover:bg-red-700 rounded-md text-white font-semibold"
      >
        Delete This File
      </button>
    </form>

  </div>
</body>
</html>
